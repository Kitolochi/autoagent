[project]
name = "autoagent"
version = "0.1.0"
description = "Autonomous agent engineering"
requires-python = ">=3.12"
dependencies = [
    "openai-agents",
    "claude-agent-sdk",
    "python-dotenv",
    "pandas",
    "openpyxl",
    "numpy",
    "harbor",
    "openai>=2.30.0",
]
