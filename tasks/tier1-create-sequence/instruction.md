Create a new sequence in the current Premiere Pro project named exactly "Test Sequence" using default settings.

Steps:
1. Check the current project state with `get_project_info`.
2. Create the sequence using `create_sequence` with the name "Test Sequence".
3. Verify the sequence exists by calling `list_sequences`.
